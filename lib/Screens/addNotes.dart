import 'package:flutter/material.dart';

class addNotes extends StatelessWidget {
  const addNotes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("addNotes"));
  }
}
