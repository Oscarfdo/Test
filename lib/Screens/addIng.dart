import 'package:flutter/material.dart';

class addIng extends StatelessWidget {
  const addIng({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("addInd"));
  }
}
